<apex:page standardController="Account" extensions="CreateRootCertController" action="{!createRootCert}"> 
      <apex:pageMessages />
      <apex:form >
      <div style="padding-left:20px;">
      <br/>
   <apex:commandButton action="{!cancel}" value="Go Back" rendered="{!showBackButton}" />
   </div>
   </apex:form>
</apex:page>